import SectionContainer from '../SectionContainer';

const AboutContainer = () => {
	return (
		<SectionContainer id="about" title="درباره من">
			<p>{'تست تست تست تست'}</p>
		</SectionContainer>
	);
};

export default AboutContainer;
